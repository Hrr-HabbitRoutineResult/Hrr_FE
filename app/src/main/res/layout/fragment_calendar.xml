<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 상단 헤더 영역 -->
    <include
        android:id="@+id/layout_record_header"
        layout="@layout/layout_record_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"/>

    <!-- 커스텀 캘린더 영역 -->
    <com.example.hrr_android.challenge.ui.record.progress.calendar.CustomCalendarView
        android:id="@+id/view_calendar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="35dp"
        app:layout_constraintTop_toBottomOf="@id/layout_record_header"/>

    <!-- 새로고침 버튼 -->
    <ImageButton
        android:id="@+id/btn_calendar_refresh"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/btn_refresh_floating"
        android:background="@null"
        app:layout_constraintBottom_toTopOf="@id/layout_challenge_button_complete"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="20dp"/>

    <!-- 하단 버튼 영역 -->
    <include
        android:id="@+id/layout_challenge_button_complete"
        layout="@layout/layout_challenge_button_complete"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>