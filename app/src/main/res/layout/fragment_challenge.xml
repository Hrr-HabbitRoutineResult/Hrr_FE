<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".challenge.ui.detail.ChallengeFragment">

    <ScrollView
        android:id="@+id/sv_challenge_content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/ll_challenge_buttons">

        <LinearLayout
            android:id="@+id/ll_challenge_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- 상단 헤더 영역 -->
            <include layout="@layout/layout_challenge_header"/>

            <!-- 프로필 영역 -->
            <include layout="@layout/layout_challenge_profile"/>

            <!-- 중간 구분선 -->
            <View
                android:layout_width="match_parent"
                android:layout_height="10dp"
                android:background="@color/grey_50"/>

            <!-- 챌린지 규칙 영역 -->
            <include
                android:id="@+id/layout_challenge_rule"
                layout="@layout/layout_challenge_rule"/>

        </LinearLayout>

    </ScrollView>

    <!-- 하단 버튼 영역 -->
    <include
        android:id="@+id/ll_challenge_buttons"
        layout="@layout/layout_challenge_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>