<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="70dp"
    android:background="@color/white">

    <!-- 프로필 이미지 -->
    <ImageView
        android:id="@+id/iv_challenge_profile_image"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/img_profile"
        android:layout_marginStart="16dp"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="15dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- 사용자 닉네임 -->
    <TextView
        android:id="@+id/tv_challenge_profile_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="최루루"
        android:textSize="13sp"
        android:fontFamily="@font/font_style"
        android:textFontWeight="500"
        android:textColor="@color/text_primary"
        android:includeFontPadding="false"
        android:layout_marginStart="10dp"
        app:layout_constraintStart_toEndOf="@id/iv_challenge_profile_image"
        app:layout_constraintTop_toTopOf="@id/iv_challenge_profile_image"
        app:layout_constraintBottom_toBottomOf="@id/iv_challenge_profile_image" />

    <!-- 사용자 레벨 -->
    <TextView
        android:id="@+id/tv_challenge_profile_level"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="골드"
        android:textSize="13sp"
        android:fontFamily="@font/font_style"
        android:textFontWeight="400"
        android:textColor="@color/text_secondary"
        android:includeFontPadding="false"
        android:layout_marginStart="5dp"
        app:layout_constraintStart_toEndOf="@id/tv_challenge_profile_name"
        app:layout_constraintTop_toTopOf="@id/tv_challenge_profile_name"
        app:layout_constraintBottom_toBottomOf="@id/tv_challenge_profile_name" />

    <ImageView
        android:id="@+id/iv_challenge_profile_arrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_challenge_arrow"
        android:layout_marginEnd="2dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>